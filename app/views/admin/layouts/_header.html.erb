<header id="header" class="header fixed-top d-flex align-items-center">
  <div class="d-flex align-items-center justify-content-between">
    <%= link_to admin_dashboard_path, class: "logo d-flex align-items-center" do %>
      <%= image_tag("/assets/img/logo.png", alt: "") %>
      <span class="d-none d-lg-block"><%= t("dashboard.logo") %></span>
    <% end %>
  </div>
  <nav class="header-nav ms-auto">
    <ul class="d-flex align-items-center">
      <li class="nav-item dropdown pe-3">
        <a class="nav-link nav-profile d-flex align-items-center pe-0" href="#" data-toggle="dropdown">
          <%= t("language.language" )%>
        </a>
        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow profile">
          <li>
            <%= link_to t("language.english"), url_for(locale: "en"), class: "dropdown-item" %>
          </li>
          <li>
            <%= link_to t("language.vietnamese"), url_for(locale: "vi"), class: "dropdown-item" %>
          </li>
        </ul>
      </li>
      <li class="nav-item dropdown pe-3">
        <a class="nav-link nav-profile d-flex align-items-center pe-0" href="#" data-toggle="dropdown">
          <%= image_tag current_user.avatar.variant(:avatar_icon), alt: "Profile", class: "rounded-circle" if current_user.avatar&.attached? %>
          <span class="d-none d-md-block dropdown-toggle ps-2">
            <%= current_user.username %>
          </span>
        </a>
        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow profile">
          <li>
            <%= link_to signout_path, data: { turbo_method: :delete }, class: "dropdown-item d-flex align-items-center" do %>
              <i class="bi bi-box-arrow-right"></i>
              <span><%= t("user.headers.account_link.logout")%></span>
            <% end %>
          </li>
        </ul>
      </li>
      <li class="nav-item dropdown pe-3">
        <a class="nav-link nav-profile d-flex align-items-center pe-0" href="#" data-toggle="dropdown">
          Language
        </a>
        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-arrow profile">
          <li>
            <%= link_to t("language.english"), url_for(locale: "en"), class: "dropdown-item" %>
          </li>
          <li>
            <%= link_to t("language.vietnamese"), url_for(locale: "vi"), class: "dropdown-item" %>
          </li>
        </ul>
      </li>
    </ul>
  </nav>
</header>
